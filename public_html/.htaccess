Options -Indexes

<IfModule mod_rewrite.c>
    RewriteEngine On
    # Hanya izinkan POST ke folder process/, kecuali logout (GET)
    RewriteCond %{REQUEST_METHOD} !POST
    RewriteCond %{REQUEST_URI} !/process/logout\.php$ [NC]
    RewriteRule ^process/ - [F]
</IfModule>
